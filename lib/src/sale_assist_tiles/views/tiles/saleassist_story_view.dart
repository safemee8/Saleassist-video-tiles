import 'package:flutter/widgets.dart';

class SaleassistStoryView extends StatelessWidget {
  const SaleassistStoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
