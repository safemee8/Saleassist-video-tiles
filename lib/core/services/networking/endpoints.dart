class Endpoints {
  // sample endpoint -
  //! "/login"
  //! if any path params - "/text/{userId}/{key}"

  // video call related

  // GET
  static String getShorts = "source/{playListId}/shorts";
}
